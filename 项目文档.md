# Modbus 通讯助手项目文档

## 1. 项目概述

Modbus 通讯助手是一款使用 Qt Widgets 开发的 Windows 工业通讯调试工具，面向设备调试、协议联调、教学演示和现场排障场景。

软件同时支持 Modbus RTU、Modbus ASCII、Modbus TCP 和内部虚拟通道，可在主站与从站模式之间切换，并提供数据解析、从站数据仿真、原始报文、通讯日志、定时轮询以及亮暗主题等功能。

| 项目 | 内容 |
| --- | --- |
| 软件名称 | Modbus 通讯助手 |
| 当前版本 | 1.0.0 |
| 开发语言 | C++17 |
| UI 框架 | Qt Widgets |
| 支持协议 | Modbus RTU、Modbus ASCII、Modbus TCP、内部虚拟通道 |
| 工作模式 | 主站、从站 |
| 目标平台 | Windows 64 位 |
| 构建方式 | CMake 或 qmake |

## 2. 主要功能

### 2.1 通讯能力

- 自动扫描本机串口。
- 内置免驱动虚拟通道，可让本机多个软件实例直接进行 Modbus RTU 主从通讯。
- 支持常用波特率、数据位、校验位、停止位和流控配置。
- 支持 Modbus RTU 的 CRC16 自动生成与校验。
- 支持 Modbus ASCII 的 LRC 自动生成与校验。
- 支持 Modbus TCP 的 MBAP 事务头生成与解析。
- TCP 主站模式可连接远程设备。
- TCP 从站模式可监听指定 IP 地址和端口。
- 串口、TCP 与内部虚拟通道均支持通讯日志和异常提示。

### 2.2 主站功能

- 读取线圈、离散输入、保持寄存器和输入寄存器。
- 写入单个或多个线圈。
- 写入单个或多个保持寄存器。
- 支持响应超时设置。
- 支持定时轮询。
- 自动检查从站地址、功能码、报文长度和校验结果。
- 自动解析 Modbus 异常响应。

### 2.3 从站功能

- 内置 65536 个线圈。
- 内置 65536 个离散输入。
- 内置 65536 个保持寄存器。
- 内置 65536 个输入寄存器。
- 可设置本站地址、数据区、显示起始地址和显示数量。
- 表格模式下可直接编辑从站数据。
- 收到主站写入请求后自动更新数据区。
- 支持按指定地址范围自动生成从站数据。
- 寄存器支持随机数、正弦波、方波、三角波、递增计数、锯齿波、高斯热噪声、随机游走、脉冲和阻尼正弦。
- 线圈与离散输入支持随机状态、流水灯、闪烁、交替翻转、全部接通和全部断开。
- 支持串口从站、TCP 从站和内部虚拟通道从站。

### 2.4 数据解析显示

数据解析区域提供两种显示方式，可通过右上角“显示方式”下拉框切换。

#### 表格模式

保留传统数据表格，显示地址、十进制、十六进制、二进制或状态。该模式适合查看大量数据和编辑从站数据。

#### 面板模式

- 线圈和离散输入显示为圆形 ON/OFF 指示灯。
- ON 使用绿色指示灯，OFF 使用灰色指示灯。
- 指示灯下方显示对应 Modbus 地址。
- 寄存器显示为独立数值框，同时显示十进制和十六进制值。
- 寄存器框下方显示对应 Modbus 地址。
- 数据按多行多列排列，列数会根据窗口宽度自动调整。
- 数据较多时可使用滚动条浏览。

显示模式会自动保存，下次启动软件时继续使用上次选择。

#### 趋势曲线

- 主站成功读取功能码 `01`、`02`、`03` 或 `04` 后，软件会追加一条采样记录。
- 每个返回地址对应一条曲线，横轴为采样序号，纵轴为当前值；寄存器用于观察数值变化，线圈和离散输入用于观察 0/1 状态。
- 单个请求最多绘制前 8 个地址，避免大量曲线重叠影响辨识；完整数据仍保留在数据解析页。
- 更换起始地址或读取数量后，软件会自动开始新的曲线组，避免不同数据点混在同一张图中。
- “暂停”只停止追加曲线采样，不影响通讯和数据解析；“清空曲线”会删除当前历史。
- “保留”可设置每条曲线保留 `20`～`500` 个采样点，鼠标移动到曲线上可查看采样序号和各地址精确值。

### 2.5 辅助功能

- 亮色与暗色工业主题切换。
- 自动记住主题设置。
- 软件多开，可同时调试多个端口或设备。
- 原始十六进制报文发送。
- 最多保留 5000 行通讯日志。
- 日志可清空或保存到文件。
- 自动保存窗口位置和常用通讯参数。

## 3. 支持的功能码

| 功能码 | 功能 | 主站 | 从站 | 单次最大数量 |
| --- | --- | --- | --- | --- |
| `01` | 读线圈 | 支持 | 支持 | 2000 位 |
| `02` | 读离散输入 | 支持 | 支持 | 2000 位 |
| `03` | 读保持寄存器 | 支持 | 支持 | 125 个 |
| `04` | 读输入寄存器 | 支持 | 支持 | 125 个 |
| `05` | 写单个线圈 | 支持 | 支持 | 1 位 |
| `06` | 写单个寄存器 | 支持 | 支持 | 1 个 |
| `0F` | 写多个线圈 | 支持 | 支持 | 1968 位 |
| `10` | 写多个寄存器 | 支持 | 支持 | 123 个 |

## 4. 界面说明

### 4.1 顶部区域

- “暗色主题/亮色主题”：切换软件整体主题。
- “软件多开”：启动一个独立软件实例。
- 连接状态标签：显示当前协议、主从站状态和连接状态。

快捷键：

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl+Shift+T` | 切换亮色/暗色主题 |
| `Ctrl+Shift+N` | 新开软件实例 |
| `Ctrl+Enter` | 发送主站请求 |
| `F5` | 刷新串口列表 |

### 4.2 通讯设置区域

选择协议和工作模式后，软件会自动显示相应参数：

- RTU/ASCII：显示串口、波特率、数据位、校验位、停止位和流控。
- TCP：显示目标或监听 IP 地址以及 TCP 端口。
- 内部虚拟通道：显示通道名称，不显示无效的串口参数；从站创建通道，主站加入同名通道。
- 主站：按钮用于打开串口、建立 TCP 连接或加入内部通道。
- 从站：按钮用于打开串口、启动 TCP 监听或创建内部通道。

### 4.3 左侧控制区域

- 主站模式下配置从站地址、功能码、起始地址、数量和写入值。
- 从站模式下配置本站地址、数据区和查看范围。

### 4.4 右侧功能页

- 数据解析 / 数据仿真：主站查看解析结果，从站查看或编辑仿真数据。
- 从站模式下，“自动数据”工具条位于数据仿真页标题下方、显示方式左侧。
- 趋势曲线：查看主站读取数据的历史变化。
- 通讯日志：查看发送、接收、系统和错误日志。
- 原始报文：发送自定义 Modbus 请求。

## 5. 快速使用

### 5.1 RTU 主站读取寄存器

1. 协议选择“Modbus RTU”。
2. 工作模式选择“主站”。
3. 选择串口并设置与设备一致的串口参数。
4. 点击“打开串口”。
5. 设置从站地址。
6. 功能码选择“03 - 读保持寄存器”。
7. 设置起始地址和数量。
8. 点击“发送请求”。
9. 在数据解析区查看结果。

### 5.2 ASCII 主站

1. 协议选择“Modbus ASCII”。
2. 设置串口参数并打开串口。
3. 按正常主站方式设置请求。
4. 软件会自动完成 ASCII 编码和 LRC 校验。

### 5.3 TCP 主站

1. 协议选择“Modbus TCP”。
2. 工作模式选择“主站”。
3. 输入设备 IP 地址和端口，默认端口为 `502`。
4. 点击连接按钮。
5. 设置单元标识、功能码、起始地址和数量。
6. 发送请求并查看解析结果。

### 5.4 从站仿真

1. 选择 RTU、ASCII、TCP 或内部虚拟通道。
2. 工作模式选择“从站”。
3. 设置本站地址。
4. 选择线圈、离散输入、保持寄存器或输入寄存器数据区。
5. 在表格模式中双击数值编辑，或在面板模式中点击线圈指示灯、输入寄存器数值。
6. 如需自动变化数据，在左侧设置显示起始地址和显示数量，再在“自动数据”中选择生成模式，然后勾选“启用自动仿真”。
7. 打开串口、启动 TCP 监听或创建内部通道。
8. 主站请求到达后，软件会自动响应并记录日志。

自动数据仿真只修改当前选中数据区和左侧显示范围。寄存器仿真值固定限制在 `0`～`1000`，更新周期固定为 500 ms，周期型波形固定为 10 s；递增计数和随机游走仍可设置步长。随机位模式可设置接通概率，流水灯可设置移动方向。新增高斯热噪声、随机游走、脉冲和阻尼正弦，适合模拟传感器、报警和回落过程。切换数据区、显示范围或主从模式时，正在运行的仿真会停止，避免数据写入错误区域。

停止仿真后保留最后一次生成值；点击“恢复手动值”可恢复本次启用前保存的范围快照。运行期间该范围的表格和面板控件不可手动编辑。主站写入请求仍会正常响应，但下一次定时更新会按当前生成模式覆盖仿真范围。

### 5.5 内部虚拟通道

1. 点击“软件多开”，准备主站和从站两个实例。
2. 两个实例的协议都选择“内部虚拟通道”，并输入完全相同的通道名称。
3. 从站实例选择“从站”，设置本站地址和数据区，然后点击“创建通道”。
4. 主站实例选择“主站”，点击“加入通道”。
5. 连接后即可按 Modbus RTU 帧格式发送、响应、轮询和查看趋势曲线。

内部通道只监听本机回环地址，不需要驱动、管理员权限或重启。它不会创建系统 `COMx` 端口，第三方串口软件无法看到该通道。

## 6. 地址与数据格式

### 6.1 地址规则

请求参数中的“起始地址”按 Modbus 报文规定使用从 `0` 开始的协议偏移地址；数据解析区则换算成 PLC 常用参考地址，便于与设备手册对照：

| 数据区 | 协议偏移地址 0 的显示 | 协议偏移地址 1 的显示 |
| --- | --- | --- |
| 线圈 | `00001` | `00002` |
| 离散输入 | `10001` | `10002` |
| 输入寄存器 | `30001` | `30002` |
| 保持寄存器 | `40001` | `40002` |

例如，设备手册中的保持寄存器 `40001`，发送请求时通常仍输入起始地址 `0`，解析结果中会显示为 `40001`。不同厂家可能采用不同的扩展地址规则，调试前应确认设备手册说明。

### 6.2 写入格式

- 单个线圈：输入 `0`、`1`、`OFF` 或 `ON`。
- 多个线圈：使用空格、逗号或分号分隔，例如 `1, 0, 1, 1`。
- 单个寄存器：输入 `0` 到 `65535`，也可使用 `0x` 十六进制格式。
- 多个寄存器：例如 `100, 200, 0x1234`。

### 6.3 原始报文

原始报文输入支持带空格或不带空格的十六进制字符串，例如：

```text
01 03 00 00 00 0A
```

软件会根据当前协议自动追加 CRC、生成 ASCII LRC 或封装 MBAP 事务头。

## 7. 构建环境

### 7.1 依赖

- Windows 10/11 64 位。
- Qt 5.15+ 或 Qt 6.x。
- Qt Widgets 模块。
- Qt SerialPort 模块。
- Qt Network 模块。
- 支持 C++17 的编译器。
- CMake 3.16 或更高版本。

### 7.2 使用 Qt Creator

1. 使用 Qt Creator 打开根目录的 `CMakeLists.txt`。
2. 选择包含 SerialPort 和 Network 模块的 Desktop Kit。
3. 选择 Release 配置。
4. 配置、构建并运行。

### 7.3 使用 CMake

```powershell
cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH="D:\Qt\6.10.2\mingw_64"
cmake --build build
```

若 MinGW 在中文路径下构建失败，可将工程复制到纯英文路径，或使用英文盘符映射后再构建。

### 7.4 使用 qmake

```powershell
qmake ModbusSerialAssistant.pro
mingw32-make release
```

## 8. Windows 发布与单文件打包

### 8.1 普通发布目录

使用 Qt 自带工具复制运行库：

```powershell
windeployqt --release --no-translations ModbusSerialAssistant.exe
```

普通发布时必须将 EXE、Qt DLL 和插件目录一起分发。

### 8.2 单文件版本

项目提供单文件打包脚本：

```powershell
powershell -ExecutionPolicy Bypass -File .\packaging\onefile\build-onefile.ps1
```

输出文件：

```text
dist\ModbusSerialAssistant_OneFile.exe
```

单文件版本会在启动时将依赖释放到临时目录，目标电脑不需要安装 Qt。首次启动速度可能略慢于普通版本。

## 9. 项目目录结构

```text
Modbus串口助手/
├─ assets/                         应用图标资源
├─ packaging/
│  ├─ create-app-icon.ps1         ICO 生成脚本
│  └─ onefile/
│     └─ build-onefile.ps1        单文件打包脚本
├─ styles/
│  ├─ icons/                      主题控件图标
│  ├─ industrial.qss              亮色工业主题
│  └─ industrial-dark.qss         暗色工业主题
├─ app_icon.rc                    Windows EXE 图标资源
├─ CMakeLists.txt                 CMake 工程配置
├─ ModbusSerialAssistant.pro      qmake 工程配置
├─ main.cpp                       程序入口
├─ mainwindow.h                   主窗口声明
├─ mainwindow.cpp                 UI、通讯和协议实现
├─ slavedatasimulator.h           从站数据生成模式与参数声明
├─ slavedatasimulator.cpp         位状态和寄存器波形生成引擎
├─ trendchart.h                   趋势曲线控件声明
├─ trendchart.cpp                 趋势曲线绘制和采样历史
├─ resources.qrc                  Qt 资源清单
├─ README.md                      项目简介
└─ 项目文档.md                    完整项目文档
```

构建产物通常位于 `build/` 和 `dist/`，这两个目录已被 Git 忽略。

## 10. 配置保存

软件使用 `QSettings` 保存以下内容：

- 窗口位置和大小。
- 亮色或暗色主题。
- 表格或面板显示模式。
- 当前协议和主从站模式。
- 串口参数。
- TCP 地址和端口。
- 请求参数。
- 从站数据查看范围。
- 从站自动仿真的模式和生成参数；仿真范围始终跟随显示范围，固定周期不保存（启动时默认关闭）。
- 趋势曲线历史采样点数。
- 超时和轮询间隔。

配置由 Windows 和 Qt 自动管理，不需要手动创建配置文件。

## 11. 常见问题

### 11.1 找不到串口

- 检查 USB 转串口设备是否连接。
- 检查驱动程序是否正确安装。
- 点击“刷新”重新扫描串口。
- 确认串口没有被其他软件占用。

### 11.2 打开串口失败

- 关闭可能占用串口的其他程序。
- 检查串口号是否正确。
- 尝试拔插设备后重新刷新。

### 11.3 无法创建或加入内部通道

- 确认主站和从站输入的通道名称完全一致，名称区分大小写。
- 必须先在从站实例中创建通道，再由主站加入。
- 同一通道只允许一个从站和一个主站；若提示名称被占用，请关闭之前创建该通道的实例或更换名称。
- 通道名称只能包含中文、字母、数字、下划线和短横线，最长 32 个字符。

### 11.4 一直响应超时

- 检查从站地址是否正确。
- 检查波特率、数据位、校验位和停止位。
- 检查 RS-485 的 A/B 线是否接反。
- 检查起始地址和功能码是否符合设备手册。
- 适当提高响应超时时间。

### 11.5 CRC 或 LRC 校验失败

- 检查串口参数是否一致。
- 检查线路干扰、接地和终端电阻。
- 确认当前选择的协议与设备一致。

### 11.6 TCP 无法连接

- 检查 IP 地址和端口。
- 检查设备是否启用 Modbus TCP 服务。
- 检查防火墙和网络连通性。
- 常用 Modbus TCP 端口为 `502`，但设备可能使用自定义端口。

### 11.7 如何在面板模式修改从站数据

- 线圈或离散输入：点击圆形 `ON/OFF` 指示灯即可切换状态。
- 保持寄存器或输入寄存器：点击数值框，输入 `0`～`65535`，按 `Enter` 或移开焦点后生效。
- 主站模式下的面板仅用于显示响应结果，不允许修改设备数据。

### 11.8 为什么从站数据无法手动修改

- 检查“自动数据”中的“启用自动仿真”是否已勾选。
- 自动仿真运行时，指定地址范围由仿真器控制，表格和面板编辑会暂时锁定。
- 取消勾选会停止生成并保留末值；需要回到启用前的数据时，点击“恢复手动值”。

## 12. 安全提示

标准 Modbus 协议本身不提供身份认证和传输加密。使用 Modbus TCP 时，不建议将设备或本软件的监听端口直接暴露到不受信任的公网。生产环境应使用网络隔离、防火墙、VPN 或工业安全网关进行保护。

## 13. 发布检查清单

- Release 模式编译成功。
- RTU、ASCII、TCP 和内部虚拟通道启动正常。
- 主站与从站切换正常。
- 从站各位模式和寄存器模式生成正常，停止与恢复手动值正常。
- 表格模式和面板模式显示正常。
- 线圈 ON/OFF 指示灯颜色正常。
- 寄存器数值和地址显示正常。
- 亮色与暗色主题正常。
- 软件多开正常。
- EXE 图标正确。
- 单文件版本在无 Qt 环境的电脑上启动正常。
- 发布附件包含版本号和更新说明。

## 14. 版本记录

### 1.0.0

- 实现 Modbus RTU、ASCII 和 TCP。
- 实现主站和从站模式。
- 支持八种常用功能码。
- 增加工业亮色和暗色主题。
- 增加软件多开。
- 增加表格和面板两种数据解析模式。
- 增加线圈指示灯和寄存器多行多列显示。
- 增加主站趋势曲线、采样暂停、清空和悬停读数。
- 增加免驱动的内部虚拟通道，支持本机多实例主从互通。
- 增加从站自动数据仿真，支持位状态序列和寄存器波形生成。
- 提供 Windows 单文件打包流程。
